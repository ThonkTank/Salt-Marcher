package features.catalog.application;

/** Stable typed action vocabulary shared by definitions, renderer, and workspace. */
public enum CatalogActionId {
    OPEN,
    ADD_TO_ENCOUNTER,
    ADD_TO_SCENE,
    USE_AS_ENCOUNTER_SOURCE,
    SET_FOCUSED_SCENE_LOCATION,
    CREATE
}
