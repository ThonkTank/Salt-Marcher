#!/usr/bin/env bash
set -euo pipefail

while read -r local_ref local_sha remote_ref remote_sha; do
    if [[ "$remote_ref" == "refs/heads/main" ]]; then
        echo "Blocked push: direct pushes to main are disabled. Open a pull request instead." >&2
        exit 1
    fi
done

exit 0
