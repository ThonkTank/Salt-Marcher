/**
 * Terrain Constants
 *
 * Re-exports from terrain-registry for backward compatibility.
 * @see terrain-registry.ts for the single source of truth.
 */

export {
	TERRAIN_TYPES,
	TERRAIN_TYPES_WITH_ALIASES,
	TERRAIN_ALIASES,
	normalizeTerrainId,
} from './terrain-registry';
