/**
 * Library Adapter Index
 *
 * Re-exports library store and view.
 * Note: Types (EntityType, IndexEntry, etc.) are now in schemas/library.ts
 *
 * @module adapters/library
 */

export { LibraryStore } from './library-store';
export { LibraryView, LIBRARY_VIEW_TYPE } from './library-view';
