interface:
  display_name: "Domain Layer"
  short_description: "Enforce SaltMarcher domain-layer boundaries"
  default_prompt: "Use $domain-layer before work under src/domain/** so the change follows the SaltMarcher domain-layer standard."

policy:
  allow_implicit_invocation: true
