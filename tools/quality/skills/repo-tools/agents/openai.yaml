interface:
  display_name: "Repo Tools"
  short_description: "Route SaltMarcher agents to repo-local developer tools and their governing skills"
  default_prompt: "Use $repo-tools when a SaltMarcher task needs an optional repo-local developer tool and its governing evidence strength."

policy:
  allow_implicit_invocation: true
