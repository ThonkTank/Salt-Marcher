interface:
  display_name: "View Layer MVVM Alias"
  short_description: "Apply SaltMarcher's canonical view-layer rules"
  default_prompt: "Use $view-layer-mvvm before work under src/view/** so the change follows SaltMarcher's canonical view-layer standard and reusable slotcontent topology."

policy:
  allow_implicit_invocation: true
