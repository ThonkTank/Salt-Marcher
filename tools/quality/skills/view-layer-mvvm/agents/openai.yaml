interface:
  display_name: "View Layer MVVM"
  short_description: "Enforce SaltMarcher cockpit MVVM contribution boundaries"
  default_prompt: "Use $view-layer-mvvm before planning, implementing, refactoring, or reviewing work under src/view/** so the change follows SaltMarcher's cockpit MVVM standard with *Contribution shell adapters, *ViewModel presentation state, and passive JavaFX views."

policy:
  allow_implicit_invocation: true
