<jqassistant-rules xmlns="http://schema.jqassistant.org/rule/v2.9"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://schema.jqassistant.org/rule/v2.9 https://jqassistant.github.io/jqassistant/current/schema/jqassistant-rule-v2.9.xsd">

    <group id="saltmarcher:view-architecture" severity="blocker">
        <includeConstraint refId="saltmarcher:MvvmAllowedViewBuckets"/>
        <includeConstraint refId="saltmarcher:MvvmNoLegacyBuckets"/>
        <includeConstraint refId="saltmarcher:MvvmRootOnlyViewContribution"/>
        <includeConstraint refId="saltmarcher:MvvmOneModelPerFile"/>
        <includeConstraint refId="saltmarcher:MvvmOnePanelViewPerFile"/>
        <includeConstraint refId="saltmarcher:MvvmPanelViewName"/>
        <includeConstraint refId="saltmarcher:MvvmViewRootEntrypointCount"/>
        <includeConstraint refId="saltmarcher:MvvmModelDependencies"/>
        <includeConstraint refId="saltmarcher:MvvmViewModelDependencies"/>
        <includeConstraint refId="saltmarcher:MvvmNoPrivateForeignComponentDependencies"/>
    </group>

    <concept id="saltmarcher:MvvmViewType">
        <description>Annotates compiled SaltMarcher view-layer types for the cockpit contribution MVVM harness.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type)
            WHERE type.fqn =~ 'src\\.view\\..*'
            WITH
              type,
              split(type.fqn, '.') AS segments
            SET
              type:SaltMarcher:MvvmViewType,
              type.viewBucket = CASE
                WHEN size(segments) >= 4 AND segments[2] = 'views' THEN 'VIEW'
                WHEN size(segments) >= 5 AND segments[2] IN ['tabs', 'topbar', 'state', 'details'] AND segments[4] CONTAINS 'Contribution' THEN 'CONTRIBUTION'
                WHEN size(segments) >= 5 AND segments[2] IN ['tabs', 'topbar', 'state', 'details'] AND segments[4] CONTAINS 'ViewModel' THEN 'MODEL'
                WHEN size(segments) >= 5 AND segments[2] IN ['tabs', 'topbar', 'state', 'details'] AND segments[4] CONTAINS 'View' THEN 'VIEW'
                ELSE 'LEGACY'
              END,
              type.relativePath = replace(type.fqn, '.', '/') + '.java'
            RETURN
              type AS MvvmViewType
        ]]></cypher>
    </concept>

    <concept id="saltmarcher:MvvmRootEntrypoint">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Labels shell contribution entrypoints.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType)
            WHERE type.viewBucket = 'CONTRIBUTION'
            SET type:SaltMarcher:MvvmRootEntrypoint
            RETURN type AS MvvmRootEntrypoint
        ]]></cypher>
    </concept>

    <constraint id="saltmarcher:MvvmAllowedViewBuckets">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Cockpit MVVM topology allows active view Java only in contribution roots or reusable src/view/views.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType)
            WHERE type.viewBucket = 'LEGACY'
            RETURN DISTINCT
              type.relativePath AS Source,
              'src/view/tabs, src/view/topbar, src/view/state, src/view/details, or src/view/views' AS ExpectedDirection,
              type.fqn AS ActualDirection,
              'Active view Java must use the cockpit contribution topology.' AS Details
            ORDER BY Source
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmNoLegacyBuckets">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Component-local view buckets are migration debt and forbidden for active Java.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType)
            WHERE type.viewBucket = 'LEGACY'
            RETURN DISTINCT
              type.relativePath AS Source,
              'src/view/tabs, src/view/topbar, src/view/state, src/view/details, or src/view/views' AS ExpectedDirection,
              type.fqn AS ActualDirection,
              'Legacy component-local View/ViewModel/api/assembly-style topology is not active target code.' AS Details
            ORDER BY Source
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmRootOnlyViewContribution">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>View contributions are the only shell-facing view-layer entrypoints.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType)
            WHERE
              type.viewBucket = 'CONTRIBUTION'
              AND NOT type.name CONTAINS '$'
              AND NOT type.name ENDS WITH 'Contribution'
            RETURN DISTINCT
              type.relativePath AS Source,
              '*Contribution' AS ExpectedDirection,
              type.fqn AS ActualDirection,
              'Each shell-facing view-layer entrypoint must be one contribution.' AS Details
            ORDER BY Source
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmOneModelPerFile">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Each contribution or ViewModel source file owns exactly one top-level type.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType)
            WHERE type.viewBucket IN ['CONTRIBUTION', 'MODEL']
              AND NOT type.name CONTAINS '$'
            WITH type.relativePath AS source, collect(DISTINCT type.name) AS typeNames
            WHERE size(typeNames) <> 1
            RETURN
              source AS Source,
              'exactly one top-level contribution or ViewModel type' AS ExpectedDirection,
              reduce(actual = '', typeName IN typeNames |
                CASE WHEN actual = '' THEN typeName ELSE actual + ', ' + typeName END) AS ActualDirection,
              'A contribution or ViewModel file must define exactly one top-level type.' AS Details
            ORDER BY Source
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmPanelViewName">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Passive panel views name their fixed shell surface.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType {viewBucket: 'VIEW'})
            WHERE
              NOT type.name CONTAINS '$'
              AND NOT (
                (type.fqn =~ 'src\\.view\\.views\\..*' AND type.name ENDS WITH 'View')
                OR
                type.name ENDS WITH 'ControlsView'
                OR type.name ENDS WITH 'MainView'
                OR type.name ENDS WITH 'DetailsView'
                OR type.name ENDS WITH 'StateView'
                OR type.name ENDS WITH 'TopBarView'
              )
            RETURN DISTINCT
              type.relativePath AS Source,
              '*ControlsView, *MainView, *DetailsView, *StateView, or *TopBarView' AS ExpectedDirection,
              type.fqn AS ActualDirection,
              'Passive view names must identify the shell surface they render.' AS Details
            ORDER BY Source
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmOnePanelViewPerFile">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Each passive view source file owns exactly one top-level panel fragment.</description>
        <cypher><![CDATA[
            MATCH (type:Java:Type:SaltMarcher:MvvmViewType {viewBucket: 'VIEW'})
            WHERE NOT type.name CONTAINS '$'
            WITH type.relativePath AS source, collect(DISTINCT type.name) AS typeNames
            WHERE size(typeNames) <> 1
            RETURN
              source AS Source,
              'exactly one top-level passive panel view type' AS ExpectedDirection,
              reduce(actual = '', typeName IN typeNames |
                CASE WHEN actual = '' THEN typeName ELSE actual + ', ' + typeName END) AS ActualDirection,
              'A passive view file must define exactly one panel fragment.' AS Details
            ORDER BY Source
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmViewRootEntrypointCount">
        <requiresConcept refId="saltmarcher:MvvmRootEntrypoint"/>
        <description>At least one shell contribution must exist.</description>
        <cypher><![CDATA[
            MATCH (root:Java:Type:SaltMarcher:MvvmRootEntrypoint)
            WITH count(root) AS rootCount
            WHERE rootCount = 0
            RETURN
              'src/view/tabs, src/view/topbar, or src/view/state' AS Source,
              'at least one shell contribution' AS ExpectedDirection,
              'none' AS ActualDirection,
              'The shell needs discoverable contributions.' AS Details
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmNoPrivateForeignComponentDependencies">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Passive panel views do not depend on contributions or ViewModels.</description>
        <cypher><![CDATA[
            MATCH (source:Java:Type:SaltMarcher:MvvmViewType)-[:DEPENDS_ON]->(target:Java:Type:SaltMarcher:MvvmViewType)
            WHERE source.viewBucket = 'VIEW' AND target.viewBucket <> 'VIEW'
            RETURN DISTINCT
              source.relativePath AS Source,
              coalesce(target.relativePath, target.fqn) AS Target,
              'passive views may depend only on other passive views' AS ExpectedDirection,
              source.fqn + ' -> ' + target.fqn AS ActualDirection,
              'Passive views must remain shell-free, domain-free, and ViewModel-free.' AS Details
            ORDER BY Source, Target
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, Target, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmModelDependencies">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>Contributions may bind passive views, use shell API, and call only domain public boundaries.</description>
        <cypher><![CDATA[
            MATCH (source:Java:Type:SaltMarcher:MvvmViewType {viewBucket: 'CONTRIBUTION'})-[:DEPENDS_ON]->(target:Java:Type)
            WHERE
              target.fqn =~ 'bootstrap\\..*'
              OR target.fqn =~ 'src\\.data\\..*'
              OR target.fqn =~ 'shell\\.host\\..*'
              OR (target.fqn =~ 'shell\\..*' AND NOT target.fqn =~ 'shell\\.api\\..*')
              OR (
                target.fqn =~ 'src\\.domain\\..*'
                AND NOT target.fqn =~ 'src\\.domain\\.[^.]+\\.[^.]+ApplicationService(\\$.*)?'
                AND NOT target.fqn =~ 'src\\.domain\\.[^.]+\\.api\\..+'
              )
              OR (
                target.fqn =~ 'src\\.view\\..*'
                AND NOT target.viewBucket IN ['CONTRIBUTION', 'MODEL', 'VIEW']
              )
            RETURN DISTINCT
              source.relativePath AS Source,
              coalesce(target.relativePath, target.fqn) AS Target,
              'shell.api, owning ViewModels, passive views, and domain public boundaries only' AS ExpectedDirection,
              source.fqn + ' -> ' + target.fqn AS ActualDirection,
              'Contributions must not depend on old view topology, data, bootstrap, shell internals, or domain internals.' AS Details
            ORDER BY Source, Target
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, Target, ActualDirection"/>
    </constraint>

    <constraint id="saltmarcher:MvvmViewModelDependencies">
        <requiresConcept refId="saltmarcher:MvvmViewType"/>
        <description>ViewModels stay independent from shell APIs and concrete views.</description>
        <cypher><![CDATA[
            MATCH (source:Java:Type:SaltMarcher:MvvmViewType {viewBucket: 'MODEL'})-[:DEPENDS_ON]->(target:Java:Type)
            WHERE
              target.fqn =~ 'bootstrap\\..*'
              OR target.fqn =~ 'src\\.data\\..*'
              OR target.fqn =~ 'shell\\..*'
              OR (
                target.fqn =~ 'src\\.domain\\..*'
                AND NOT target.fqn =~ 'src\\.domain\\.[^.]+\\.[^.]+ApplicationService(\\$.*)?'
                AND NOT target.fqn =~ 'src\\.domain\\.[^.]+\\.api\\..+'
              )
              OR (
                target.fqn =~ 'src\\.view\\..*'
                AND NOT target.viewBucket = 'MODEL'
              )
            RETURN DISTINCT
              source.relativePath AS Source,
              coalesce(target.relativePath, target.fqn) AS Target,
              'JavaFX bindable state, sibling ViewModels, and domain public boundaries only' AS ExpectedDirection,
              source.fqn + ' -> ' + target.fqn AS ActualDirection,
              'ViewModels must not depend on shell APIs, concrete views, data, bootstrap, or domain internals.' AS Details
            ORDER BY Source, Target
        ]]></cypher>
        <report primaryColumn="Source" keyColumns="Source, Target, ActualDirection"/>
    </constraint>

</jqassistant-rules>
