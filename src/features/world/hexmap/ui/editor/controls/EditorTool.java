package features.world.hexmap.ui.editor.controls;

/** Aktives Werkzeug im Karteneditor. Bestimmt die Interpretation von Klick/Drag-Events auf dem Hex-Grid. */
public enum EditorTool {
    SELECT,
    TERRAIN_BRUSH
}
