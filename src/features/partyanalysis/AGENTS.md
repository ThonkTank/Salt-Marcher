# Party Analysis Feature

| Public API | Internal | Allowed consumers |
| --- | --- | --- |
| `features.partyanalysis.api` | `application`, `model`, `repository`, `service` | `features.encounter`, `features.party`, importer maintenance flows, `ui.bootstrap` |
