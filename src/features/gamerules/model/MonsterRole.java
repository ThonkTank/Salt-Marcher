package features.gamerules.model;

/** Tactical role a monster fills in an encounter (e.g. front-line tank, ranged artillery). */
public enum MonsterRole {
    BRUTE,
    ARTILLERY,
    LEADER,
    SOLDIER,
    ARCHER,
    SKIRMISHER,
    SUPPORT,
    CONTROLLER,
    MINION,
    TANK
}
